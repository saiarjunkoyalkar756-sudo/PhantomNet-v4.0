import React from 'react';

const TerminalChat = () => (
  <section className="rounded-lg border border-border bg-panel-solid p-6">
    <h2 className="text-lg font-semibold text-primary">Governed Advisory AI Integration Pending</h2>
    <p className="mt-3 text-sm text-text-secondary leading-6">
      This dashboard does not accept copilot prompts, return AI explanations, retrieve threat status, inspect agents, run playbooks, or execute defensive actions. The previous conversational client could imply operational authority without tenant-scoped evidence, policy gates, authorization, or auditable advisory boundaries.
    </p>
    <p className="mt-3 text-sm text-text-secondary leading-6">
      Any future advisory integration must use evidence-minimized, tenant-scoped, provenance-linked inputs; remain policy-gated and non-executing; identify recommendations as advisory; and preserve approval-bound containment with HMAC-signed audit, verification, and rollback outside the chat interface.
    </p>
  </section>
);

export default TerminalChat;
