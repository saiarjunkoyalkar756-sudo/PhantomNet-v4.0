[pytest]
pythonpath =
    .
    phantomnet_agent
norecursedirs = PhantomNet-v3.0
asyncio_mode = auto
filterwarnings =
    ignore:Diffie-Hellman over finite fields .*:cryptography.utils.CryptographyDeprecationWarning
